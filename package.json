{
  "name": "share-type",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run-super": "tsc -v && ts-node src/superstruct/index.ts",
    "run-yup": "tsc -v && ts-node src/yup/index.ts",
    "run-ajv": "tsc -v && ts-node src/ajv/index.ts",
    "generate-json-schema": "ts-node generate.ts",
    "tsc": "tsc -v && tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ajv": "^8.11.0",
    "json-schema-to-typescript": "^11.0.1",
    "superstruct": "^0.16.0",
    "typescript": "4.5.2",
    "typescript-json-schema": "^0.54.0",
    "yup": "^1.0.0-beta.4"
  }
}
